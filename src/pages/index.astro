---
// 获取客户端的Accept-Language头
const preferredLang = Astro.request.headers.get('accept-language');

if(preferredLang != null && preferredLang.includes('zh'))
{
    return Astro.redirect('/zh-cn');
}

return Astro.redirect('/en');
---